name: Release

# Tag a signed `vX.Y.Z` on main to publish a release.
#   git tag -s v0.2.8 -m "Release v0.2.8" && git push origin v0.2.8
# A `v*` tag (a) cuts a GitHub Release, (b) builds + pushes GHCR images for
# argus-server + argus-web, and (c) publishes the `argus-netbox` package to PyPI via
# trusted publishing (OIDC — no token).

on:
  push:
    tags:
      - "v*"

# This workflow stays on GitHub-hosted runners. The self-hosted fleet is arm64 Linux with no
# docker CLI on the runner's PATH, and pypa/gh-action-pypi-publish ships an amd64-only image,
# so the first release run on that fleet failed in the images and pypi jobs (2026-09-22). GHCR
# images are therefore built for linux/amd64, as for every earlier release.

# Least privilege by default; each job widens only what it needs.
permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true

  images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - name: argus-server
            context: server
          - name: argus-web
            context: web
    steps:
      - uses: actions/checkout@v7
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Derive image tags + labels
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository_owner }}/${{ matrix.name }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest
      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.context }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  pypi:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Build the argus distribution
        working-directory: server
        run: |
          python -m pip install --upgrade build
          python -m build
      - name: Publish to PyPI (trusted publishing)
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: server/dist
