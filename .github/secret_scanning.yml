# Suppress secret scanning on documentation and example code.
# Docs / examples may contain illustrative tokens that aren't real credentials.
# Real secrets live outside the repo (env vars / secret files), never committed.
paths-ignore:
  - 'docs/**'
  - '**/.env.example'
