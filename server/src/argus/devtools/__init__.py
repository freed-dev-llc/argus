"""Argus dev/maintenance tooling — repeatable repo workflows encoded as deterministic,
testable commands so an agent (or human) runs one command instead of re-deriving the
procedure each time.

Not part of the runtime/product surface (the MCP network-automation tools); these are
manifest-driven helpers for maintaining the repo itself. See ``release.py``.
"""
