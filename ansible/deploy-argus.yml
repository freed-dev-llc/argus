---
# Deploy the Argus stack to the hosts in the `argus_deploy` group.
#   ansible-playbook deploy-argus.yml        # inventory/hosts.yml is a default source (ansible.cfg)
- name: Deploy Argus
  hosts: argus_deploy
  gather_facts: true
  roles:
    - argus_deploy
