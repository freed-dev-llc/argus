---
galaxy_info:
  author: Jon Freed
  description: Deploy the self-contained Argus stack (NetBox + Argus server + web) via Docker Compose.
  license: Apache-2.0
  min_ansible_version: "2.15"
  galaxy_tags:
    - netbox
    - docker
    - argus
    - automation
dependencies: []
