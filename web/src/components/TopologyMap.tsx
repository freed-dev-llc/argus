export function TopologyMap() {
  return (
    <div className="placeholder">
      <p>Topology visualization is planned (P3).</p>
      <p className="muted">
        Will render NetBox cabling / LLDP neighbors and the IPAM hierarchy — likely via
        react-flow or cytoscape.
      </p>
    </div>
  )
}
